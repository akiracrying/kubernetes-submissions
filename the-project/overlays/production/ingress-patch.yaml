apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: the-project
  namespace: production
spec:
  rules:
    - host: the-project.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: the-project
                port:
                  number: 3000

